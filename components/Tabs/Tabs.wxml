<view class="tabs">
    <view class="tabs_title">
        <view class="title_item {{item.isActive?'acitve':''}}" wx:for="{{tabs}}" wx:key="id" bindtap="handleTitleChange" data-index="{{index}}">
            {{item.title}}
        </view>
    </view>
    <view class="tabs_content">
        <slot></slot>
    </view>
</view>